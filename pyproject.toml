[project]
name = "h1-polymarket"
version = "0.1.0"
description = "Pre-registered Stage 1 gate test of H1-revised: long-horizon NO-side edge in Polymarket politics markets"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
